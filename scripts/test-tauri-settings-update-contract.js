const fs=require('node:fs'),assert=require('node:assert/strict'); const settings=fs.readFileSync('desktop-tauri/src/pages/SettingsPage.tsx','utf8'),types=fs.readFileSync('desktop-tauri/src/types/index.ts','utf8'); for(const token of ['profileName','protocol','otaUseApiHost','otaTimeoutSeconds','updateChannel','autoCheckUpdates','firmwareUpdateChecks']){assert.ok(settings.includes(token));assert.ok(types.includes(token));} assert.ok(settings.includes('/usr/local/bin/arduinoOTA')); assert.ok(settings.includes('IP vagy DDNS')); console.log('OK: Settings és TypeScript konfigurációs szerződés');
