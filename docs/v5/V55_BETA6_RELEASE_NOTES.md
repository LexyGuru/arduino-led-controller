# V5.5.1 Beta.6 Release Notes

Application `5.5.1-beta.6`; firmware `5.0.0-beta.10`; Direct API `1.0.0`.

## Hotfix scope
Focused macOS Local Network OTA hotfix. OTA target selection remains unchanged.
A zero-byte TCP connect failure stops immediately instead of entering false reboot confirmation.

## Preserved platform contracts
- Theme Engine 2.5 remains unchanged.
- Update System 2.0 remains unchanged.
- LXC runtime behavior remains unchanged; application metadata follows `5.5.1-beta.6`.
- Stable `main` is not modified by this Beta.6 hotfix.

## Test Architecture V2
19 CURRENT + 23 REGRESSION.
