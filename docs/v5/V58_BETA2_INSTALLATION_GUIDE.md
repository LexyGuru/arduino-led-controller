# Arduino LED Controller V5.8 Beta.2 — Installation Guide

> **TEST BUILD ONLY — NO GITHUB RELEASE / NO TAG**

Application version: `5.8.0-beta.2`
Firmware version: `5.1.0-beta.2`
Direct API version: `1.0.0`
Channel: `beta`
Branch: `next/v5-rearchitecture`

This Beta.2 source state exists so the normal GitHub push build can create
downloadable test artifacts for real-device/mobile verification.

## Test installation

Install the artifact produced by the GitHub build for commit `5.8.0-beta.2` source state.
This is not a published GitHub Release and must not be treated as a stable build.

## Test focus

- redesigned mobile weekly schedule workspace
- seven days visible without horizontal scrolling
- selected-day event list separated from the editor
- full-viewport mobile schedule editor
- LED topology save feedback and firmware logging

## Release status

- GitHub Release: **not created**
- Git tag: **not created**
- Stable/main promotion: **not performed**
