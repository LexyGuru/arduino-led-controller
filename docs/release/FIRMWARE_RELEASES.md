# Firmware Releases

Firmware is an independent lifecycle. Each verified entry requires version, channel, UNO R4 WiFi binary and SHA-256 pair. Legacy `Arduino_LED_Controller_Firmware_BETA` remains compatibility surface until migrated.
